{
  "name": "tranzo-monorepo",
  "version": "1.0.0",
  "description": "Tranzo crypto wallet",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm run start",
    "dev": "cd backend && npm run dev"
  }
}
